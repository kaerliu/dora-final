---
description: 完成静态/网页原型后必须提供本机浏览器可打开的预览链接
alwaysApply: true
---

# 原型本地预览链接（默认）

当本次任务涉及**可浏览器查看的静态原型**（如本仓库的 `index.html`、纯 HTML/CSS/JS 演示页等）时，在交付说明中**默认**包含可直接在浏览器打开的链接，不要只描述路径让用户自己猜。

## 必须做的事

1. **启动本地静态服务**（在原型根目录或含入口 HTML 的目录执行其一即可）：
   - `python3 -m http.server 8765`
   - 或 `npx --yes serve -l 8765 .`（若环境已有 Node）
2. **在回复里写出完整 URL**，例如：`http://127.0.0.1:8765/` 或 `http://localhost:8765/index.html`。
3. **说明服务是本地的**：关闭终端或结束进程后链接会失效；若端口被占用，换用其他端口并在回复中写清实际端口。

## 可选补充

- 若用户无法起服务，可同时给出 **file://** 绝对路径作为备用（例如 `file:///Users/.../index.html`），并注明部分浏览器对外链脚本限制更严，优先用 `http://localhost`。

## 不要求

- 不要求部署到公网；除非用户明确要求托管/分享外网链接。
